import { SshCustomizeDirective } from './ssh-customize.directive';

describe('SshCustomizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SshCustomizeDirective();
    expect(directive).toBeTruthy();
  });
});
